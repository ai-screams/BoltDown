export const STORAGE_KEYS = {
  settings: 'boltdown-settings',
  sidebarWidth: 'boltdown-sidebar-width',
  recentFiles: 'boltdown-recent-files',
  legacyTheme: 'boltdown-theme',
  findReplacePreferences: 'find-replace-preferences',
} as const

export const MEDIA_QUERIES = {
  prefersDark: '(prefers-color-scheme: dark)',
} as const
